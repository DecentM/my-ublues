modules:
  - type: rpm-ostree

    repos:
      - google-cloud-cli.repo

    keys:
      - https://packages.cloud.google.com/yum/doc/rpm-package-key.gpg

    install:
      - libxcrypt-compat
      - google-cloud-cli
