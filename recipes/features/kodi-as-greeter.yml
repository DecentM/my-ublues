modules:
  - type: systemd
    system:
      enabled:
        - kodi-standalone.service
        - kodi-standalone-restart-on-resume.service

  - type: files
    files:
      - source: kodi-as-greeter
        destination: /
